test:
  override:
    - echo "test"

deployment:
  prod:
    branch: master
    commands:
      - npm install
      - gulp build
      - aws s3 sync public/ s3://www.southsidegardencentre.co.nz.s3.amazonaws.com --delete