machine:
  node:
    version: 6.5.0

test:
  override:
    - echo "test"

deployment:
  prod:
    branch: master
    commands:
      - npm rebuild node-sass
      - gulp build
      - aws s3 sync public/ s3://www.southsidegardencentre.co.nz --region ap-southeast-2 --delete 